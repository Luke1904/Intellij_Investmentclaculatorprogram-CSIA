// Main class serves as the entry point for the application
public class Main {

    /**
     * The main method is the starting point of the program.
     * It creates an instance of the MainMenu class, which initializes and displays the main menu of the application.
     *
     * @param args Command-line arguments (not used in this application).
     */
    public static void main(String[] args) {
        // Create an instance of the MainMenu class
        MainMenu menu = new MainMenu();
    }
}